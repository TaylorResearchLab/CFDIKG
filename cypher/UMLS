# How many Concept nodes are there?    #  4,231,008
match (c:Concept)
return count(c)    

# What properties do Concepts have?     #  just a "CUI"
match (c:Concept)
return distinct keys(c)     

# How many Code nodes are there?
match (cd:Code)
return count(cd)

# How many HGNC Code nodes are there?
match(n:Code {SAB:'HGNC'}) return count(n)    # 41,638


# what type of properties do term nodes have?
match (t:Term) return distinct keys(t)     # ["SUI", "name"]
